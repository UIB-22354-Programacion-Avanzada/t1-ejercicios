package es.uib.prgava.tema1.monitor;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.fail;

/**
 * Ejercicio 1.4.4. <strong>Las pruebas son el entregable.</strong>
 *
 * <p>Escribe cuatro pruebas: los cuatro valores por defecto del <em>builder</em> comprobados con
 * un solo {@code assertAll}; que construir sin servicios lanza {@code IllegalArgumentException}
 * con el mensaje esperado; un {@code @ParameterizedTest} con {@code @ValueSource(ints = {-1, -10})}
 * para los reintentos negativos; y que la lista de servicios devuelta es inmutable.
 *
 * <p>Cuando termines, borra el método {@code pendiente}.
 */
class ConfiguracionMonitorTest {

    @Test
    void pendiente() {
        fail("TODO 1.4.4: escribe aquí las pruebas de ConfiguracionMonitor");
    }
}
