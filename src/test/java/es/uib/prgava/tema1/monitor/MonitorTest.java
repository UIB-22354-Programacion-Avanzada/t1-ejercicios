package es.uib.prgava.tema1.monitor;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.fail;

/**
 * Ejercicio 1.4.3. <strong>Las pruebas son el entregable.</strong>
 *
 * <p>Con {@link SondaProgramada} y {@link NotificadorEspia}, comprueba que:
 * dos servicios distintos alertan de forma independiente; que el orden de las alertas recibidas
 * coincide con el orden de comprobación; y que un servicio que se recupera y vuelve a fallar no
 * dispara la alerta hasta acumular de nuevo los fallos necesarios.
 *
 * <p>Ninguna prueba puede tardar más de unos milisegundos ni usar números aleatorios.
 * Cuando termines, borra el método {@code pendiente}.
 */
class MonitorTest {

    @Test
    void pendiente() {
        fail("TODO 1.4.3: escribe aquí las pruebas de Monitor con dobles de prueba");
    }
}
